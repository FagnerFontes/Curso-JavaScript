//Criar m vetor com A 5 elementos inteiros. Construir um vetor B de mesmo tipo tamanho e com os elementos do vetor A multiplocados por 2, ou seja: B[i] = A[i] * 2 

let VetorA = [1,2,3,4,5]
let VetorB = VetorA * 2 

console.log(`${VetorB}`);