//A programação Orientada a Objetos (POO) é um paradigma de programação que se baseia no conceito de "objetos". Esses objetos representam entidades do mundo real e são a base para organizar e estruturar o código de um programa. Aqui estão alguns dos principais conceitos e princípios fundamentais da POO. 

//1. Objetos: Os objetos são instâncias de classes, Eles são unidades autônomas que podem conter dados (conhecidos como atributos) e comportamento (conhecido como métodos). Por exemplo, um objeto pode representar um carro, com atributos como cor e modelo, e métodos como ligar e desligar o motor. 

//2. As classes são modelos ou planos para criar objetos. Elas definem quais atributos e métodos os objetos terão. Usando o exemplo anterior, a classe "Carro" pode ter atributos como "cor" e "modelo" e métodos como "ligar" e "desligar". 

//3. Encapsulamento: O encapsulamento é um princípio que preconiza que os detalhes internos de um lobjeto ser ocultos do mundo exterior. Isso significa que os atributos de um objeto devem ser privados ou protegidos, e os acesso deser ser feito por meio de métodos públicos. 

//4. Herança: A herança permite que uma classe (conhecida como subclasse ou classe derivada) herde atributos e métodos de outra classe (conhecida como superclasse ou classe base). Isso promove a reutiliza de código e a criação de hierarquias de classes. 

//5. Polimorfismo: Permite que objetos de diferentes classes sejam tratados de maneira uniforme. Isso é alcançadp por meio da sobrecarga de métodos (métodos com o msmo, mas com diferentes implementações) e da implementação de métodos de uma interface comum. 

//6. Abstração: A abstração é o processo de simplificar objetos complexos, extraindo apenas os detalhes relavantes. Isso ajuda a cricar modelos conceituais que representam objetos do mundo real de forma mais eficas. 

//7. Métodos e Atributos: Os métodos são funções que definem o comportamento de um objeto, enquanto os atributos são variáveis que armazenam dados relacionaos a um objeto. 

//8: Instância: Uma instância é um objeto específico criado a partir de uma classe. Cada instância lpossui seus próprios valores de atributos, mas compartilha os mesmos métodos definidos na classe. 