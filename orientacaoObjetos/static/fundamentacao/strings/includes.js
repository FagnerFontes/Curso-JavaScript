//Método includes
//Este método determina se um conjunto de caracteres pode ser encontrado dentro de outras string, retornando true ou false

//1. O código abaixo verifica se uma determinada palavra se encontra na string
/* const nome = "Fagner Fontes Batista"//Cria uma string

console.log(nome.includes("correia"));//Verifica e imprime true ou false

const numbers = [1,2,3,4,5,7]
const targetNumber = parseInt(prompt("Digite um numero"))

if(numbers.includes(targetNumber)){
    console.log("O número está presente no array");
}else{
    console.log("O número não está presente no array");
} */

//2. Crie uma função que recebe uma string como parâmetro e verifica se uma determina letra está presete nessa string

/* function checkLetter(string,letter) {
    return string.includes(letter)
}
const inputString = "Hellor Word"; 
const targetLetter = prompt("Digite uma letra")
console.log(checkLetter(inputString, targetLetter)); */

